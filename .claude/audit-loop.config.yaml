# Demo repo audit-loop config — Step 1.3 (identifier scrub) and beyond.
# The demo repo has no test surface yet (tests/ was not copied). Validation = grep gates + python syntax check.
validate_command: "bash scripts/validate_identifiers.sh"
progress_file: docs/progress.md
